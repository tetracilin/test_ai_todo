// SCAFFOLD ONLY (NLM-A03): no parsing behavior yet. Real `nlm` JSON/raw output
// parsing is implemented in NLM-A04 per the canonical plan.
//
// Kept as an empty module (rather than omitted) so the directory layout
// matches docs/adapters/creating-an-adapter.md and the canonical plan's
// Phase 1 package tree, and so NLM-A04 has an obvious place to add parse
// functions without introducing a new file.
export {};
